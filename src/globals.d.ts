/**
 * The base URL webpack resolves the plugin's own assets from. The scaffold sets
 * it at runtime from the AMD module URI, so it follows Grafana's sub-path.
 */
declare const __webpack_public_path__: string;
